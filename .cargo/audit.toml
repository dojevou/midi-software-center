# Cargo audit configuration
# https://rustsec.org/

[advisories]
# Advisories to ignore (with justification)
ignore = [
    # RUSTSEC-2024-0429: Unsoundness in glib::VariantStrIter
    # This is a transitive dependency from Tauri's GTK bindings (webkit2gtk).
    # The affected API (VariantStrIter) is not used in our codebase.
    # Waiting for gtk-rs to release a fix upstream.
    "RUSTSEC-2024-0429",
]
